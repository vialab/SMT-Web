Erik Paluka. 2013.

Turns Simple Multi-Touch's example PDEs into HTML pages.

Simple Multi-Touch needs to be put into a folder called "SMT". This folder then needs to be put into the parent directory (../) of "SMT-Web".

Run "egToHTML.bat" to remove old example HTML pages, and replace them with new versions.